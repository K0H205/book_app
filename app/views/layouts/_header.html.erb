<header class="header">
<% if logged_in? %>
  <ul>
    <li><%= link_to "LOGOUT",logout_path,method: :delete %></li>
    <li><%= link_to "MYBOOK",books_path(@current_user.id) %></li>
  </ul>
  <% end %>
</header>